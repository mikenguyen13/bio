# Posts Log

| Date | Slug | Type | Angle | Distributed | Notes |
|---|---|---|---|---|---|
| 2026-08-19 | `staggered-rollouts-break-twfe` | Evergreen | Geo rollout, TWFE bias, Callaway-Sant'Anna | pending | First post of the new cadence. Originally drafted as a trend-jack, reclassified as evergreen because the underlying result dates to 2018 to 2021. Python block shipped unverified and was **fixed on 2026-08-26**, see Amendments. Log previously dated this 2026-08-20, but the front matter says 2026-08-19. |
| 2026-08-25 | `continuous-treatment-did-in-r` | Trend-jack | Continuous-dose DiD, `contdid`, spend coefficient is not marginal ROI | pending | First post to clear the freshness rule properly: `contdid` 0.1.1 reached CRAN 2026-07-21, five weeks before publication. Dose randomized in the simulation, so the 31% overstatement is pure weighting bias rather than selection. Every block executed, no Python. Target query: difference in differences with a continuous treatment in R. |
| 2026-08-28 | `calibrate-mmm-with-incrementality-test` | Trend-jack (industry/SEO) | Feeding a geo test into an MMM as a prior does not fix a misspecified MMM | drafted | Freshness anchors: Meridian generally available Jan 2025 (19 months), IAB State of Data 2026 published Feb 2026. Fills the Friday slot with an industry post rather than an evergreen, per the one-for-one mix rule. Contrarian against the 2026 triangulation consensus, so the framing is the thing to check. Every block executed: six `stan_glm` fits in R plus one executed Meridian 1.8.0 chunk in Python. Target query: calibrate MMM with incrementality test. |


| 2026-08-28 | `unicode-normalization-and-llm-token-costs` | Evergreen (AI) | The same Korean word is 2 tokens composed and 36 decomposed, and normalization beats language choice as a cost lever | drafted | **First post in the AI lane**, written after the interaction power draft was killed for topic mix. See the DIVERSIFY rule in IDEAS.md. Corpus is UDHR Article 1 in 11 languages, vendored next to the post as `udhr_article1.tsv` written straight from the upstream XML, because the decomposed Vietnamese entry is silently recomposed by any editor or clipboard it touches. Language gap tops out near 2x once normalized. Normalization gap reaches 9.82x. Every block executed, R driving `tiktoken` through `reticulate`. Target query: unicode normalization llm token costs. |
| 2026-08-28 | `what-structured-output-costs-you-in-tokens` | Evergreen (AI) | 45% of a pretty printed JSON payload is content, and repeated key names are 69% of a minified one | drafted | Companion to the normalization post, cross-linked. Pretty JSON is 3.59x TSV for identical data. Key share is flat at 69% from about 20 records up, so it never amortizes, unlike the schema which costs 188 tokens once. Carries a "What this post did not measure" section, because only cost was measured and shortening keys may cost accuracy. Every block executed. Target query: llm structured output token cost. |
| 2026-09-01 | `how-to-benchmark-r-code-correctly` | Evergreen (coding and tooling) | Block execution order hands the first-listed expression a double-digit artifact, and randomizing removes it | drafted | **First post in the coding and tooling lane**, taken after the last three rows read AI, AI, marketing. See the DIVERSIFY rule in IDEAS.md. Racing a function against a byte-identical copy of itself, `bench::mark`-style block ordering (`a a a a b b b b`) put the first-listed slot 10.3% off the truth in the knit session and 98% of races landed the same way, while `microbenchmark`'s default random ordering returned a clean null every time. The artifact's size is a property of the session, not the code: eight fresh `callr` sessions averaged 1.3% against the knit session's 10.3%. Mechanism is periodic GC spikes on a drifting baseline, aliased onto the comparison by block ordering, and the effect survives whether you summarise with a median or a mean. Every number in the prose is an inline expression reading the computed objects, so the text re-derives itself on each render and cannot contradict the tables. Every block executed, R plus one executed Python chunk. Target query: how to benchmark R code correctly. |

| 2026-09-08 | `synthetic-respondents-conjoint-analysis` | Trend-jack (AI + marketing research) | A synthetic panel with correct mean price sensitivity and compressed heterogeneity passes validation and then underprices by 26% | **LIVE 2026-09-08** | Tuesday slot. Freshness anchor: Maier et al. semantic similarity rating, arXiv 2510.08338, October 2025, 11 months old, clears the rule. Lanes 3 and 4 were the underweighted ones and this sits in 3 crossed with marketing research. The design holds `E|b_price|` fixed at 0.050 across every panel while shrinking the spread, so the quantity a validation report checks is identical by construction. Synthetic panel predicted the human holdout product share at 0.2472 against an actual 0.2478, closer than the human panel's own model at 0.2545. Then optimal price 43.5 against a truth of 58.5, premium segment 14.0% against 34.1%. Cross-checked in Python by 24-node 3D Gauss-Hermite quadrature, which reproduced the R Monte Carlo optimum at all five compression levels exactly. Every block executed, R with `logitr` plus one executed `{python}` chunk. Target query: synthetic respondents conjoint analysis. Published same day on Mike's approval, breaking an 11-day silence since the MMM post. Re-rendered at publish time with every number byte-identical to the draft render, confirming the fixed seeds are genuinely reproducible. Figure confirmed present in `public/` at `figs/sweepplot-1.png`, so it avoids the site-wide broken-image bug. |

| 2026-09-12 | `customer-lifetime-value-in-r` | Evergreen (marketing and measurement) | A third of the top decile by historical spend has already left, and the median one then buys nothing | **LIVE 2026-09-12** | Fills the Friday slot a day late, per the recovery rule of not doubling up. Marketing was the underweighted lane at one post against three for AI. **First post built on real third-party data rather than simulation**: CDNOW, 2,357 customers, 6,696 transactions, split 39 weeks calibration and 39 weeks holdout, so the ground truth is actual customer behaviour. Pareto/NBD via `CLVTools` 0.12.1 flags 76 of the top 236 customers as inactive, and their median holdout spend is 0.00 against 130.45 for the rest, with 61% buying nothing at all. Extrapolating last period overstates the holdout by 145%, and the model undershoots it by 16%, which the post states plainly. Recency effect shown in raw data with purchase count held fixed (1.48x to 2.63x). Bootstrap CI on the 3.08x gap computed in an executed Python chunk: 1.92 to 5.77. Every block executed. Target query: customer lifetime value in r. Published same day on Mike's approval. Re-rendered at publish time with every number byte-identical to the draft render. |

## Amendments

| Date | Post | What changed |
|---|---|---|
| 2026-09-08 | `synthetic-respondents-conjoint-analysis` (live) | Post-publication render fix, prose only. One sentence shipped with three bare dollar signs, which MathJax paired into an inline equation, so "worth $24 to the average buyer. Prices tested in the survey run from $25" rendered as `24totheaveragebuyer.Pricestestedinthesurveyrunfrom` with an orphaned "$85" trailing it. Rewritten to say dollars in words, which also matches the bare-number convention the rest of the post already used. No code changed and every computed number re-rendered byte-identical, verified by diff. `stylecheck.R` gained a bare-dollar check that reproduces the failure on the old text, and the other six 2026 posts were confirmed clean. |
| 2026-08-26 | `staggered-rollouts-break-twfe` (live) | Python block converted from a static fence to an executed `{python}` chunk. Output now on the page: True ATT 1.634, TWFE 0.919. Closes the last unverified block on the site. All R numbers re-rendered byte-identical. |
| 2026-08-26 | `calibrate-mmm-with-incrementality-test` (draft) | Added the Meridian repo link, a "What this post did not do" scope section, and an executed Meridian 1.8.0 chunk showing its lognormal ROI prior differs from the post's normal prior by at most 0.055 across the central 95%. |
| 2026-08-26 | `calibrate-mmm-with-incrementality-test` (live) | Post-publication prose correction. No code changed and no computed output changed, verified byte-identical against an independent re-run. Fixed the front matter summary conflating the experiment's answer with the truth, confronted the `spec_first` video result of 3.02 that the section had hidden behind a mean, corrected the sweep claim that social moves toward 1.50 when it overshoots to 1.74, reconciled the 3.77 the precision weighting predicts against the 3.55 the fit returned, disclosed that the geo experiment is simulated without adstock and that this flatters the experiment, and filled the three-to-five hole in the disagreement test rule. Details in REVIEW-NOW.md. |
