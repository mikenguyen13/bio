<?xml version="1.0" encoding="utf-8" standalone="yes" ?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>marketing analytics | Mike Nguyen</title>
    <link>https://mikenguyen.netlify.app/tag/marketing-analytics/</link>
      <atom:link href="https://mikenguyen.netlify.app/tag/marketing-analytics/index.xml" rel="self" type="application/rss+xml" />
    <description>marketing analytics</description>
    <generator>Wowchemy (https://wowchemy.com)</generator><language>en-us</language><copyright>© Mike Nguyen 2026</copyright><lastBuildDate>Sat, 12 Sep 2026 00:00:00 +0000</lastBuildDate>
    <image>
      <url>https://mikenguyen.netlify.app/media/social_sharing_image.png</url>
      <title>marketing analytics</title>
      <link>https://mikenguyen.netlify.app/tag/marketing-analytics/</link>
    </image>
    
    <item>
      <title>Customer Lifetime Value in R: A Third of Your Best Customers Have Already Left</title>
      <link>https://mikenguyen.netlify.app/post/customer-lifetime-value-in-r/</link>
      <pubDate>Sat, 12 Sep 2026 00:00:00 +0000</pubDate>
      <guid>https://mikenguyen.netlify.app/post/customer-lifetime-value-in-r/</guid>
      <description>


&lt;p&gt;Sort your customers by what they have spent and take the top decile. That list is the one
retention budgets get spent against. On real transaction data with a real holdout period, a
third of that list had almost certainly stopped buying before the list was drawn, and the
median customer in that third went on to spend nothing at all.&lt;/p&gt;
&lt;p&gt;The problem is that in a non-contractual business nobody cancels. There is no churn event to
observe, so a customer who has quietly left looks exactly like a customer who is between
purchases. Historical spend cannot tell them apart, because it throws away the one thing that
distinguishes them, which is when the spending happened.&lt;/p&gt;
&lt;pre class=&#34;r&#34;&gt;&lt;code&gt;library(CLVTools)
data(&amp;quot;cdnow&amp;quot;)

SPLIT &amp;lt;- as.Date(&amp;quot;1997-10-01&amp;quot;)   # 39 weeks of history, 39 weeks of holdout
d &amp;lt;- as.data.frame(cdnow)
d$Date &amp;lt;- as.Date(d$Date)
cal &amp;lt;- subset(d, Date &amp;lt;= SPLIT)

c(customers = length(unique(d$Id)),
  transactions = nrow(d),
  weeks = round(as.numeric(max(d$Date) - min(d$Date)) / 7))
##    customers transactions        weeks 
##         2357         6696           78&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The CDNOW panel is the standard public test bed for this: every customer made a first
purchase in the opening quarter of 1997, and the panel runs to mid 1998. Splitting it in half
gives 39 weeks to learn from and 39 weeks of real behaviour to be judged against. Nothing
below is simulated.&lt;/p&gt;
&lt;div id=&#34;what-extrapolation-does&#34; class=&#34;section level2&#34;&gt;
&lt;h2&gt;What extrapolation does&lt;/h2&gt;
&lt;p&gt;The naive forecast writes itself when the two windows are the same length. Whatever a
customer spent in the last 39 weeks, assume they spend again in the next 39.&lt;/p&gt;
&lt;pre class=&#34;r&#34;&gt;&lt;code&gt;clv &amp;lt;- clvdata(cdnow, date.format = &amp;quot;ymd&amp;quot;, time.unit = &amp;quot;week&amp;quot;, estimation.split = 39,
               name.id = &amp;quot;Id&amp;quot;, name.date = &amp;quot;Date&amp;quot;, name.price = &amp;quot;Price&amp;quot;)
fit &amp;lt;- pnbd(clv)
p   &amp;lt;- as.data.frame(predict(fit, verbose = FALSE))
p$hist_spend &amp;lt;- as.numeric(tapply(cal$Price, cal$Id, sum)[as.character(p$Id)])

data.frame(
  method = c(&amp;quot;actual holdout&amp;quot;, &amp;quot;extrapolate last period&amp;quot;, &amp;quot;Pareto/NBD&amp;quot;),
  total  = round(c(sum(p$actual.period.spending), sum(p$hist_spend),
                   sum(p$predicted.period.spending))),
  error  = c(&amp;quot;&amp;quot;, sprintf(&amp;quot;%+.0f%%&amp;quot;, 100 * (sum(p$hist_spend) /
                                           sum(p$actual.period.spending) - 1)),
             sprintf(&amp;quot;%+.0f%%&amp;quot;, 100 * (sum(p$predicted.period.spending) /
                                       sum(p$actual.period.spending) - 1))))
##                    method  total error
## 1          actual holdout  70792      
## 2 extrapolate last period 173300 +145%
## 3              Pareto/NBD  59640  -16%&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Extrapolation overshoots by 145%. The model undershoots by
16%, so it is closer but it is not free of error either, and
that is worth saying out loud before the part where it wins.&lt;/p&gt;
&lt;p&gt;That 145% also flatters the argument, so here is the correction
against my own headline. Every CDNOW customer makes a first purchase in the opening quarter,
which means the calibration window contains an acquisition purchase that the holdout window
structurally cannot. Strip each customer’s first transaction and extrapolate only repeat
spending, and the comparison becomes fair.&lt;/p&gt;
&lt;pre class=&#34;r&#34;&gt;&lt;code&gt;first_amt &amp;lt;- sapply(split(cal, cal$Id), function(z) z$Price[which.min(z$Date)])
p$repeat_spend &amp;lt;- p$hist_spend - as.numeric(first_amt[as.character(p$Id)])
fair_err &amp;lt;- 100 * (sum(p$repeat_spend) / sum(p$actual.period.spending) - 1)

c(repeat_only_forecast = round(sum(p$repeat_spend)),
  actual = round(sum(p$actual.period.spending)),
  overshoot_pct = round(fair_err))
## repeat_only_forecast               actual        overshoot_pct 
##                95540                70792                   35&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Still an overshoot, still in the same direction, but 35% rather
than 145%. Use that number as the honest one. The level is not
where this argument is strongest anyway.&lt;/p&gt;
&lt;/div&gt;
&lt;div id=&#34;the-list-that-matters&#34; class=&#34;section level2&#34;&gt;
&lt;h2&gt;The list that matters&lt;/h2&gt;
&lt;p&gt;The level is the less interesting failure. The ranking is where money actually gets
allocated. &lt;code&gt;pnbd&lt;/code&gt; returns &lt;code&gt;PAlive&lt;/code&gt;, the probability a customer is still active given how much
and how recently they bought.&lt;/p&gt;
&lt;pre class=&#34;r&#34;&gt;&lt;code&gt;top  &amp;lt;- p[order(p$hist_spend, decreasing = TRUE)[1:round(0.10 * nrow(p))], ]
dead &amp;lt;- top$PAlive &amp;lt; 0.5

data.frame(
  group = c(&amp;quot;flagged inactive&amp;quot;, &amp;quot;still active&amp;quot;),
  customers = c(sum(dead), sum(!dead)),
  mean_holdout   = round(c(mean(top$actual.period.spending[dead]),
                           mean(top$actual.period.spending[!dead])), 2),
  median_holdout = round(c(median(top$actual.period.spending[dead]),
                           median(top$actual.period.spending[!dead])), 2),
  pct_buying_nothing = round(100 * c(mean(top$actual.period.spending[dead] == 0),
                                     mean(top$actual.period.spending[!dead] == 0))))
##              group customers mean_holdout median_holdout pct_buying_nothing
## 1 flagged inactive        76        59.73           0.00                 61
## 2     still active       160       184.24         130.45                 21&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;32% of the top decile is flagged. Those customers go on to earn
13% of the decile’s holdout revenue, they spend
3.1 times less on average, and the median one spends
0 dollars against 130 for the rest. The
median is the number to look at, because the mean is held up by a handful of flagged customers
who did come back. Most of them simply do not.&lt;/p&gt;
&lt;div class=&#34;figure&#34;&gt;&lt;span style=&#34;display:block;&#34; id=&#34;fig:figure&#34;&gt;&lt;/span&gt;
&lt;img src=&#34;figs/figure-1.png&#34; alt=&#34;Within the top decile by historical spend, what customers actually went on to spend, binned by the model&#39;s probability that they were still active. The mean is noisy in the small low-probability bins because a few flagged customers did come back. The median does not move.&#34; width=&#34;1050&#34; /&gt;
&lt;p class=&#34;caption&#34;&gt;
Figure 1: Within the top decile by historical spend, what customers actually went on to spend, binned by the model’s probability that they were still active. The mean is noisy in the small low-probability bins because a few flagged customers did come back. The median does not move.
&lt;/p&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;div id=&#34;why-recency-carries-the-signal&#34; class=&#34;section level2&#34;&gt;
&lt;h2&gt;Why recency carries the signal&lt;/h2&gt;
&lt;p&gt;This is not the model asserting something. Hold purchase count fixed and let only recency
vary, and the difference is already in the raw data.&lt;/p&gt;
&lt;pre class=&#34;r&#34;&gt;&lt;code&gt;p$cal_n &amp;lt;- as.numeric(tapply(cal$Price, cal$Id, length)[as.character(p$Id)])
lastbuy &amp;lt;- tapply(cal$Date, cal$Id, max)
p$recency_wk &amp;lt;- as.numeric(SPLIT - as.Date(lastbuy[as.character(p$Id)],
                                           origin = &amp;quot;1970-01-01&amp;quot;)) / 7

do.call(rbind, lapply(2:4, function(k) {
  s &amp;lt;- subset(p, cal_n == k)
  recent &amp;lt;- s$recency_wk &amp;lt;= median(s$recency_wk)
  data.frame(purchases = k, customers = nrow(s),
             recent_half = round(mean(s$actual.period.spending[recent]), 2),
             stale_half  = round(mean(s$actual.period.spending[!recent]), 2),
             ratio = round(mean(s$actual.period.spending[recent]) /
                           mean(s$actual.period.spending[!recent]), 2))
}))
##   purchases customers recent_half stale_half ratio
## 1         2       438       34.80      20.03  1.74
## 2         3       214       60.89      41.08  1.48
## 3         4       101       90.62      34.46  2.63&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Two customers, both with four purchases on the books, and the one who bought more recently is
worth multiples of the one who did not. Historical spend scores them identically. That is the
whole gap, and it is why the fix is a model that uses recency rather than a better
spreadsheet.&lt;/p&gt;
&lt;p&gt;A bootstrap on the headline gap, computed in Python so the resampling is independent of the R
code above. Customers are resampled, model predictions are held fixed, so this is uncertainty
in the population difference and not in the fitted parameters.&lt;/p&gt;
&lt;pre class=&#34;python&#34;&gt;&lt;code&gt;import numpy as np

spend = np.array(r.top_spend, dtype=float)
flag  = np.array(r.top_flag, dtype=bool)
rng   = np.random.default_rng(20260912)

obs = spend[~flag].mean() / spend[flag].mean()
idx = rng.integers(0, len(spend), size=(4000, len(spend)))
reps = []
for row in idx:
    s, f = spend[row], flag[row]
    if f.sum() &amp;gt; 0 and (~f).sum() &amp;gt; 0:
        reps.append(s[~f].mean() / s[f].mean())
lo, hi = np.percentile(reps, [2.5, 97.5])
print(f&amp;quot;active / inactive mean spend ratio: {obs:.2f}&amp;quot;)
## active / inactive mean spend ratio: 3.08
print(f&amp;quot;95% bootstrap interval: {lo:.2f} to {hi:.2f}  ({len(reps)} resamples)&amp;quot;)
## 95% bootstrap interval: 1.92 to 5.77  (4000 resamples)&lt;/code&gt;&lt;/pre&gt;
&lt;/div&gt;
&lt;div id=&#34;what-this-does-not-fix&#34; class=&#34;section level2&#34;&gt;
&lt;h2&gt;What this does not fix&lt;/h2&gt;
&lt;p&gt;The model is not a forecast of the total, and the table above says so: it came in
16% under the real holdout figure. Pareto/NBD is built to rank
customers and to estimate who is still there, and that is what it is good at.&lt;/p&gt;
&lt;p&gt;The ranking gain is also narrower than the headline suggests. Targeting the top decile by
model prediction rather than by historical spend captures only a couple of extra points of
holdout revenue, because the two lists overlap heavily. The value is not a different list, it
is knowing which names on the same list to stop spending on.&lt;/p&gt;
&lt;pre class=&#34;r&#34;&gt;&lt;code&gt;capture &amp;lt;- function(score, frac) {
  sel &amp;lt;- order(score, decreasing = TRUE)[1:ceiling(frac * nrow(p))]
  100 * sum(p$actual.period.spending[sel]) / sum(p$actual.period.spending)
}
data.frame(targeted = c(&amp;quot;top 5%&amp;quot;, &amp;quot;top 10%&amp;quot;, &amp;quot;top 20%&amp;quot;),
           by_historical_spend = round(sapply(c(.05, .10, .20),
                                              function(f) capture(p$hist_spend, f)), 1),
           by_model = round(sapply(c(.05, .10, .20),
                                   function(f) capture(p$predicted.period.spending, f)), 1))
##   targeted by_historical_spend by_model
## 1   top 5%                32.0     37.0
## 2  top 10%                48.1     50.2
## 3  top 20%                66.2     67.1&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;And none of this applies to a contractual business. If customers sign up and cancel, you
observe churn directly, a survival model is the right tool, and the whole latent-attrition
apparatus is unnecessary. This is a fix for retail, ecommerce, and anywhere else that people
leave without telling you.&lt;/p&gt;
&lt;p&gt;One dataset, one product category, one period. CDNOW is the standard benchmark precisely
because it is public and well studied, not because 1997 compact disc buyers resemble your
customers.&lt;/p&gt;
&lt;/div&gt;
&lt;div id=&#34;what-to-do-on-monday&#34; class=&#34;section level2&#34;&gt;
&lt;h2&gt;What to do on Monday&lt;/h2&gt;
&lt;ol style=&#34;list-style-type: decimal&#34;&gt;
&lt;li&gt;Stop ranking customers on historical spend alone. It scores a departed customer and a loyal
one identically, and the departed one is cheaper to identify than to win back.&lt;/li&gt;
&lt;li&gt;Split your history and score yourself against a real holdout before trusting any lifetime
value number. The calculation above took one split and one model fit.&lt;/li&gt;
&lt;li&gt;Report the probability a customer is still active next to their value, not instead of it.
The decision is different for a high-value customer at 0.2 than at 0.9.&lt;/li&gt;
&lt;li&gt;Check whether your business is contractual first. If you can see cancellations, none of
this is your problem.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The latent attrition models behind this, and where they break, are covered in
&lt;a href=&#34;https://bookdown.org/mike/marketing_research/&#34;&gt;Marketing Research&lt;/a&gt;.&lt;/p&gt;
&lt;/div&gt;
</description>
    </item>
    
    <item>
      <title>Difference-in-Differences With a Continuous Treatment in R: The Dose Coefficient Is Not Marginal ROI</title>
      <link>https://mikenguyen.netlify.app/post/continuous-treatment-did-in-r/</link>
      <pubDate>Tue, 25 Aug 2026 00:00:00 +0000</pubDate>
      <guid>https://mikenguyen.netlify.app/post/continuous-treatment-did-in-r/</guid>
      <description>


&lt;p&gt;Most measurement questions are not “did we run the campaign.” They are “we spent somewhere
between two and forty thousand dollars per market, so what did the next dollar buy.” That is
difference-in-differences with a continuous treatment, and the obvious move is to put spend on
the right-hand side of a two-way fixed effects regression and read the coefficient as marginal
return.&lt;/p&gt;
&lt;p&gt;That coefficient is not marginal return. On data where I chose the true answer myself, it
misses by 31%, and there is no confounding anywhere in the simulation to blame it on.&lt;/p&gt;
&lt;div id=&#34;a-dose-where-the-truth-is-known&#34; class=&#34;section level2&#34;&gt;
&lt;h2&gt;A dose where the truth is known&lt;/h2&gt;
&lt;p&gt;Four thousand markets, four periods, treatment switching on in period 3. Sixty percent of
markets get a spend increase drawn uniformly between 0.1 and 1.0, and the rest get nothing and
serve as controls.&lt;/p&gt;
&lt;p&gt;The important detail is that I assign the dose &lt;strong&gt;at random&lt;/strong&gt;. No market chooses its budget, so
there is no selection, no reverse causality, and no correlation between spend and pre-existing
trends. Every problem below survives in a world where the dose was randomized.&lt;/p&gt;
&lt;p&gt;The true response to dose is concave, which is just the formal way of saying diminishing
returns:&lt;/p&gt;
&lt;pre class=&#34;r&#34;&gt;&lt;code&gt;library(contdid)
library(fixest)
library(dplyr)
set.seed(20260825)

n_mkt &amp;lt;- 4000

# 40% of markets get nothing. The rest get a random spend increase.
dose &amp;lt;- ifelse(rbinom(n_mkt, 1, 0.6) == 1, runif(n_mkt, 0.1, 1.0), 0)
mkt_fe &amp;lt;- rnorm(n_mkt, mean = 100, sd = 10)

# TRUE dose-response and its derivative
att_fn  &amp;lt;- function(d) 10 * d - 5 * d^2   # effect of dose d versus zero
acrt_fn &amp;lt;- function(d) 10 - 10 * d        # marginal return at dose d

# Draw the noise once so later variations differ only in the response shape
noise &amp;lt;- rnorm(n_mkt * 4)

panel &amp;lt;- expand.grid(id = 1:n_mkt, t = 1:4) |&amp;gt;
  mutate(
    dose = .env$dose[id],
    g    = ifelse(dose &amp;gt; 0, 3, 0),                  # period of first treatment
    post = as.integer(t &amp;gt;= 3 &amp;amp; dose &amp;gt; 0),
    y    = mkt_fe[id] + 2 * t + post * att_fn(dose) + .env$noise,
    dose_post = dose * post
  )&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Two numbers describe the truth, and they are not the same number.&lt;/p&gt;
&lt;pre class=&#34;r&#34;&gt;&lt;code&gt;treated_dose &amp;lt;- dose[dose &amp;gt; 0]
c(mean_ATT  = mean(att_fn(treated_dose)),    # average effect of the spend that happened
  mean_ACRT = mean(acrt_fn(treated_dose)))   # average marginal return to one more unit
##  mean_ATT mean_ACRT 
##  3.650137  4.512328&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The first is the average effect of the spend versus not spending. The second is the average
slope, which is what “what did the next dollar buy” actually means. Now the regression
everyone runs:&lt;/p&gt;
&lt;pre class=&#34;r&#34;&gt;&lt;code&gt;twfe &amp;lt;- feols(y ~ dose_post | id + t, data = panel)
twfe
## OLS estimation, Dep. Var.: y
## Observations: 16,000
## Fixed-effects: id: 4,000,  t: 4
## Standard-errors: IID 
##           Estimate Std. Error t value  Pr(&amp;gt;|t|)    
## dose_post   5.9044   0.047895 123.279 &amp;lt; 2.2e-16 ***
## ---
## Signif. codes:  0 &amp;#39;***&amp;#39; 0.001 &amp;#39;**&amp;#39; 0.01 &amp;#39;*&amp;#39; 0.05 &amp;#39;.&amp;#39; 0.1 &amp;#39; &amp;#39; 1
## RMSE: 0.877639     Adj. R2: 0.990841
##                  Within R2: 0.558868&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The coefficient is 5.90. The true average marginal return is 4.51. The regression overstates
the return on the next dollar by 31%, on randomized data.&lt;/p&gt;
&lt;/div&gt;
&lt;div id=&#34;the-weights-not-the-confounding&#34; class=&#34;section level2&#34;&gt;
&lt;h2&gt;The weights, not the confounding&lt;/h2&gt;
&lt;p&gt;Callaway, Goodman-Bacon, and Sant’Anna set this out in a paper that has circulated as
&lt;a href=&#34;https://www.nber.org/papers/w32117&#34;&gt;NBER working paper 32117&lt;/a&gt; and reached CRAN as the
&lt;a href=&#34;https://cran.r-project.org/web/packages/contdid/index.html&#34;&gt;&lt;code&gt;contdid&lt;/code&gt;&lt;/a&gt; package on 2026-07-21.
Their point separates two things that a single regression coefficient blurs together.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;ATT(d)&lt;/code&gt; is the effect of receiving dose &lt;code&gt;d&lt;/code&gt; rather than zero. &lt;code&gt;ACRT(d)&lt;/code&gt;, the average causal
response, is its derivative: the payoff to a marginal increase at dose &lt;code&gt;d&lt;/code&gt;. When treatment is a
binary switch these collapse into one quantity. When treatment is a dose they do not, and the
fixed effects coefficient estimates neither of them.&lt;/p&gt;
&lt;p&gt;What it estimates is a weighted average of &lt;code&gt;ACRT(d)&lt;/code&gt; across doses, with weights determined by
the variance of the dose variable rather than by how much spend actually sat at each level.
Those weights lean on the middle of the dose distribution and underweight the tails. If
&lt;code&gt;ACRT(d)&lt;/code&gt; is the same everywhere, any set of weights averaging to one returns the right answer
and nothing goes wrong. If &lt;code&gt;ACRT(d)&lt;/code&gt; varies with dose, the weights bite.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;ACRT(d)&lt;/code&gt; is constant only when the dose-response is a straight line. So the condition that
makes the regression trustworthy is the absence of diminishing returns, which is usually the
thing the analysis was commissioned to measure.&lt;/p&gt;
&lt;p&gt;The direction of the error follows the curvature:&lt;/p&gt;
&lt;pre class=&#34;r&#34;&gt;&lt;code&gt;bias_check &amp;lt;- function(att_fn, acrt_fn, label) {
  p &amp;lt;- expand.grid(id = 1:n_mkt, t = 1:4) |&amp;gt;
    mutate(dose = .env$dose[id],
           post = as.integer(t &amp;gt;= 3 &amp;amp; dose &amp;gt; 0),
           y = mkt_fe[id] + 2 * t + post * att_fn(dose) + .env$noise,
           dose_post = dose * post)
  b &amp;lt;- coef(feols(y ~ dose_post | id + t, data = p))[[&amp;quot;dose_post&amp;quot;]]
  truth &amp;lt;- mean(acrt_fn(treated_dose))
  data.frame(shape = label, twfe = round(b, 3), truth = round(truth, 3),
             error_pct = round(100 * (b / truth - 1), 1))
}

rbind(
  bias_check(function(d) 10 * d - 5 * d^2, function(d) 10 - 10 * d,       &amp;quot;concave&amp;quot;),
  bias_check(function(d) 6 * d,            function(d) rep(6, length(d)), &amp;quot;linear&amp;quot;),
  bias_check(function(d) 2 * d + 4 * d^2,  function(d) 2 + 8 * d,         &amp;quot;convex&amp;quot;)
)
##     shape  twfe truth error_pct
## 1 concave 5.904 4.512      30.9
## 2  linear 5.994 6.000      -0.1
## 3  convex 5.265 6.390     -17.6&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Concave dose-response, the case every media mix deck assumes, pushes the coefficient &lt;strong&gt;above&lt;/strong&gt;
the truth. Convex pulls it below. Linear is the only shape the regression handles, and there
the small residual gap is sampling noise: averaged over 200 replications the linear case
returns 5.9991 against a truth of 6.&lt;/p&gt;
&lt;p&gt;The practical reading is uncomfortable. If returns diminish and you measure them with a spend
coefficient, the estimate is biased toward telling you to spend more.&lt;/p&gt;
&lt;/div&gt;
&lt;div id=&#34;what-contdid-does-instead&#34; class=&#34;section level2&#34;&gt;
&lt;h2&gt;What &lt;code&gt;contdid&lt;/code&gt; does instead&lt;/h2&gt;
&lt;p&gt;The package estimates the whole &lt;code&gt;ATT(d)&lt;/code&gt; curve and its derivative rather than compressing them
into one slope, and it reports the aggregate with the weights you would have chosen yourself.&lt;/p&gt;
&lt;pre class=&#34;r&#34;&gt;&lt;code&gt;cd &amp;lt;- cont_did(
  yname = &amp;quot;y&amp;quot;, dname = &amp;quot;dose&amp;quot;, gname = &amp;quot;g&amp;quot;, tname = &amp;quot;t&amp;quot;, idname = &amp;quot;id&amp;quot;,
  data = panel,
  target_parameter = &amp;quot;slope&amp;quot;,      # &amp;quot;level&amp;quot; for ATT(d), &amp;quot;slope&amp;quot; for ACRT(d)
  aggregation      = &amp;quot;dose&amp;quot;,
  treatment_type   = &amp;quot;continuous&amp;quot;,
  dose_est_method  = &amp;quot;parametric&amp;quot;, # B-spline. Use &amp;quot;cck&amp;quot; for nonparametric.
  control_group    = &amp;quot;nevertreated&amp;quot;,
  num_knots = 0, degree = 3, biters = 200
)

c(ACRT = cd$overall_acrt, ATT = cd$overall_att)
##     ACRT      ATT 
## 4.608057 3.637010&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;4.61 against a truth of 4.51, and 3.64 against a truth of 3.65. The regression that ignores
curvature returned 5.90.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;num_knots&lt;/code&gt; is the one dial worth understanding. It buys flexibility in the shape at the cost
of much wider tails, and on a smooth response like this one the extra knots mostly buy noise.
I used none.&lt;/p&gt;
&lt;p&gt;The aggregate is the least interesting output, though. The curve is the thing you actually want,
because it tells you where the next dollar should go:&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;figs/acrt-curve-1.png&#34; width=&#34;1050&#34; /&gt;&lt;/p&gt;
&lt;p&gt;The dashed line is the truth, the blue line is &lt;code&gt;contdid&lt;/code&gt; with a uniform confidence band, and the
orange line is the fixed effects coefficient, which is flat by construction. The estimate runs a
little above the truth and its band widens at both ends, which is the honest report of how
little data sits out there. The shape is right, and the shape is what the budget question needs.&lt;/p&gt;
&lt;p&gt;The flat orange line crosses the truth at a dose near 0.4 and stays above it from there. At the
top of the range it still reports a marginal return close to 5.9, where the real one has fallen
to roughly zero. That is the gap that funds a campaign extension nobody should have approved.&lt;/p&gt;
&lt;/div&gt;
&lt;div id=&#34;one-caveat-that-makes-real-data-worse&#34; class=&#34;section level2&#34;&gt;
&lt;h2&gt;One caveat that makes real data worse&lt;/h2&gt;
&lt;p&gt;Everything above happened under randomized dose. Real spend is chosen, and larger markets tend
to get larger budgets for reasons that also drive their trends. That adds a second problem on
top of the weighting one: comparing a high-dose market to a low-dose market is itself a
difference-in-differences comparison, and it is only valid under what the paper calls strong
parallel trends, meaning markets that chose different doses would have responded identically to
the same dose. Selection into dose is exactly what breaks that. The 31% here is the error you
cannot escape even when assignment is clean.&lt;/p&gt;
&lt;/div&gt;
&lt;div id=&#34;what-to-do-on-monday&#34; class=&#34;section level2&#34;&gt;
&lt;h2&gt;What to do on Monday&lt;/h2&gt;
&lt;ol style=&#34;list-style-type: decimal&#34;&gt;
&lt;li&gt;&lt;strong&gt;Say which estimand you want out loud.&lt;/strong&gt; “Effect of the spend we did” is &lt;code&gt;ATT(d)&lt;/code&gt;. “Return
on the next dollar” is &lt;code&gt;ACRT(d)&lt;/code&gt;. They are different numbers and the second is usually the
one driving the budget.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Do not read a spend coefficient as marginal ROI.&lt;/strong&gt; It is a variance-weighted average of
marginal returns, and it equals marginal ROI only if returns are constant.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;If you believe in diminishing returns, you have already assumed the regression is
biased.&lt;/strong&gt; Those two beliefs cannot both be held at once.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Report the dose-response curve.&lt;/strong&gt; Deciding where to spend needs &lt;code&gt;ACRT(d)&lt;/code&gt; at the doses you
are choosing between, not one average across all of them.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Check the support.&lt;/strong&gt; The curve is only estimable over doses you observed. &lt;code&gt;contdid&lt;/code&gt;
defaults to trimming the tails for good reason, so do not extrapolate past them.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Remember randomization does not rescue you here.&lt;/strong&gt; It fixes selection into dose. It does
not fix the weights.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Binarizing the dose into treated versus untreated is a defensible fallback when the dose
distribution is thin, but it answers a smaller question. If the decision is how much to spend
rather than whether to spend, the curve is the deliverable.&lt;/p&gt;
&lt;hr /&gt;
&lt;p&gt;&lt;em&gt;Identification under continuous and multi-valued treatments, and the parallel trends
assumptions each design needs, are covered in the
&lt;a href=&#34;https://bookdown.org/mike/data_analysis/sec-difference-in-differences.html&#34;&gt;difference-in-differences chapter&lt;/a&gt;
of A Guide on Data Analysis.&lt;/em&gt;&lt;/p&gt;
&lt;/div&gt;
</description>
    </item>
    
    <item>
      <title>If You Roll Out Market by Market, Your Lift Estimate Is Probably Too Low</title>
      <link>https://mikenguyen.netlify.app/post/staggered-rollouts-break-twfe/</link>
      <pubDate>Wed, 19 Aug 2026 00:00:00 +0000</pubDate>
      <guid>https://mikenguyen.netlify.app/post/staggered-rollouts-break-twfe/</guid>
      <description>


&lt;p&gt;Geo experiments are having a moment. With a large share of individual-level conversion
signal now gone, measurement has moved back up to the market level: split the country into
regions, turn the campaign on in some of them, compare. It survives signal loss because it
never needed to track a person in the first place.&lt;/p&gt;
&lt;p&gt;But almost nobody turns a campaign on everywhere at once. Budget, inventory, and legal
approvals mean markets go live in waves: ten DMAs in March, ten more in May, the rest in
July. That is a &lt;em&gt;staggered rollout&lt;/em&gt;, and it quietly breaks the regression that most teams
reach for first.&lt;/p&gt;
&lt;p&gt;Here is the problem on data where I know the right answer, because I generated it.&lt;/p&gt;
&lt;div id=&#34;a-rollout-where-the-truth-is-known&#34; class=&#34;section level2&#34;&gt;
&lt;h2&gt;A rollout where the truth is known&lt;/h2&gt;
&lt;p&gt;Three hundred markets, twenty weeks. Markets go live in three waves (weeks 8, 12, and 16),
and a quarter of them never launch at all. The campaign builds. It lifts sales by 0.30 in
its first week and grows with exposure, which is what a real campaign does as awareness
accumulates.&lt;/p&gt;
&lt;pre class=&#34;r&#34;&gt;&lt;code&gt;library(data.table)
library(fixest)
library(did)

set.seed(2026)
n_geo &amp;lt;- 300
n_per &amp;lt;- 20

geo &amp;lt;- data.table(
  geo_id = 1:n_geo,
  g      = sample(c(8, 12, 16, Inf), n_geo, replace = TRUE, prob = rep(.25, 4)),
  geo_fe = rnorm(n_geo, 0, 1)
)

panel &amp;lt;- CJ(geo_id = 1:n_geo, week = 1:n_per)
panel &amp;lt;- merge(panel, geo, by = &amp;quot;geo_id&amp;quot;)

panel[, exposure := fifelse(is.finite(g) &amp;amp; week &amp;gt;= g, week - g, NA_real_)]
panel[, tau      := fifelse(is.na(exposure), 0, 0.30 * (exposure + 1))]
panel[, treated  := as.integer(!is.na(exposure))]
panel[, sales    := 10 + geo_fe + 0.05 * week + tau + rnorm(.N, 0, 0.5)]

true_att &amp;lt;- panel[treated == 1, mean(tau)]&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Averaged over every treated market-week, the true average effect is
&lt;strong&gt;1.65&lt;/strong&gt;. That is the number any honest estimator should return.&lt;/p&gt;
&lt;/div&gt;
&lt;div id=&#34;the-default-answer-is-far-too-low&#34; class=&#34;section level2&#34;&gt;
&lt;h2&gt;The default answer is far too low&lt;/h2&gt;
&lt;p&gt;The standard move is a two-way fixed effects regression with market effects, week effects,
and one treatment dummy:&lt;/p&gt;
&lt;pre class=&#34;r&#34;&gt;&lt;code&gt;m_twfe &amp;lt;- feols(sales ~ treated | geo_id + week, data = panel)
twfe   &amp;lt;- coef(m_twfe)[[&amp;quot;treated&amp;quot;]]
twfe
## [1] 0.9809783&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code&gt;## True ATT : 1.652
## TWFE     : 0.981
## Bias     : -40.6%&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The regression is fine. The standard error is small. Nothing in the output looks wrong. And
the answer is off by roughly &lt;strong&gt;41%&lt;/strong&gt;
in the direction that costs you budget. It makes the campaign look weaker than it is.&lt;/p&gt;
&lt;/div&gt;
&lt;div id=&#34;why-it-happens&#34; class=&#34;section level2&#34;&gt;
&lt;h2&gt;Why it happens&lt;/h2&gt;
&lt;p&gt;Two-way fixed effects doesn’t compute one comparison. It computes a weighted average of
&lt;em&gt;every&lt;/em&gt; two-group, two-period comparison available in the panel, and some of those
comparisons use &lt;strong&gt;already-treated markets as the control group&lt;/strong&gt;.&lt;/p&gt;
&lt;p&gt;That is the flaw. When the wave-2 markets go live in week 12, the regression happily uses
the wave-1 markets, treated since week 8 and by now four weeks into a &lt;em&gt;growing&lt;/em&gt; effect, as a
comparison. Their outcome is still rising because of the campaign. Subtracting that rise
treats real campaign lift as if it were the baseline trend, and the estimate gets dragged
down. &lt;a href=&#34;https://doi.org/10.1016/j.jeconom.2021.03.014&#34;&gt;Goodman-Bacon (2021)&lt;/a&gt; decomposed
exactly this. Those bad comparisons can even enter with negative weight, which is how a
genuinely positive campaign can produce a negative coefficient.&lt;/p&gt;
&lt;p&gt;The condition that triggers it is not exotic. You need only two things: &lt;strong&gt;staggered timing&lt;/strong&gt;
and &lt;strong&gt;effects that change with exposure&lt;/strong&gt;. Every campaign rollout I have seen has both.&lt;/p&gt;
&lt;/div&gt;
&lt;div id=&#34;the-fix&#34; class=&#34;section level2&#34;&gt;
&lt;h2&gt;The fix&lt;/h2&gt;
&lt;p&gt;&lt;a href=&#34;https://doi.org/10.1016/j.jeconom.2020.12.001&#34;&gt;Callaway and Sant’Anna (2021)&lt;/a&gt; estimate the
effect separately for each cohort at each period, using only clean controls, meaning markets
that are not yet live, and then aggregate. In R that is the &lt;code&gt;did&lt;/code&gt; package:&lt;/p&gt;
&lt;pre class=&#34;r&#34;&gt;&lt;code&gt;panel[, gvar := fifelse(is.finite(g), g, 0)]   # 0 codes &amp;quot;never treated&amp;quot;

cs &amp;lt;- att_gt(
  yname = &amp;quot;sales&amp;quot;, tname = &amp;quot;week&amp;quot;, idname = &amp;quot;geo_id&amp;quot;, gname = &amp;quot;gvar&amp;quot;,
  data = as.data.frame(panel),
  control_group = &amp;quot;notyettreated&amp;quot;,
  bstrap = TRUE, cband = FALSE
)

simple &amp;lt;- aggte(cs, type = &amp;quot;simple&amp;quot;, na.rm = TRUE)&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code&gt;## True ATT      : 1.652
## TWFE          : 0.981  (bias -40.6%)
## Callaway-SA   : 1.713  (bias +3.7%)&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;That recovers the truth to within sampling error. The small remaining gap is weighting.
&lt;code&gt;aggte(type = &#34;simple&#34;)&lt;/code&gt; weights group-time cells by their size, which is not identical to
averaging over treated market-weeks. It is noise rather than bias, about
1.1 standard errors,
against a TWFE gap of
21.&lt;/p&gt;
&lt;/div&gt;
&lt;div id=&#34;read-the-curve-not-the-scalar&#34; class=&#34;section level2&#34;&gt;
&lt;h2&gt;Read the curve, not the scalar&lt;/h2&gt;
&lt;p&gt;The single number was never the interesting part. Because the effect grows, what you
actually want is the shape:&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;figs/event-study-1.png&#34; width=&#34;1050&#34; /&gt;&lt;/p&gt;
&lt;p&gt;Pre-launch estimates sit flat at zero, which is the design check. After launch the effect
climbs steadily, and the flat grey line shows what the single TWFE coefficient claims
instead. A campaign that pays back over six weeks and one that pays back instantly can
produce the same scalar. Only one of them justifies a longer flight.&lt;/p&gt;
&lt;/div&gt;
&lt;div id=&#34;the-same-trap-in-python&#34; class=&#34;section level2&#34;&gt;
&lt;h2&gt;The same trap in Python&lt;/h2&gt;
&lt;p&gt;Nothing here is an R problem. It is a data problem, and the identical bias appears in
Python. For a balanced panel you don’t even need a package, because the two-way within
estimator is just OLS on the double-demeaned series.&lt;/p&gt;
&lt;pre class=&#34;python&#34;&gt;&lt;code&gt;import numpy as np, pandas as pd

rng = np.random.default_rng(2026)
n_geo, n_per = 300, 20

g      = rng.choice([8, 12, 16, np.inf], size=n_geo, p=[.25] * 4)
geo_fe = rng.normal(0, 1, n_geo)

geo_id = np.repeat(np.arange(n_geo), n_per)
week   = np.tile(np.arange(1, n_per + 1), n_geo)

df = pd.DataFrame({&amp;quot;geo_id&amp;quot;: geo_id, &amp;quot;week&amp;quot;: week,
                   &amp;quot;g&amp;quot;: g[geo_id], &amp;quot;geo_fe&amp;quot;: geo_fe[geo_id]})

df[&amp;quot;exposure&amp;quot;] = np.where(df.week &amp;gt;= df.g, df.week - df.g, np.nan)
df[&amp;quot;tau&amp;quot;]      = np.where(df.exposure.notna(), 0.30 * (df.exposure + 1), 0.0)
df[&amp;quot;treated&amp;quot;]  = df.exposure.notna().astype(int)
df[&amp;quot;sales&amp;quot;]    = (10 + df.geo_fe + 0.05 * df.week + df.tau
                  + rng.normal(0, 0.5, len(df)))

def twoway_demean(col):
    x = df[col] - df[col].mean()
    x = x - x.groupby(df.geo_id).transform(&amp;quot;mean&amp;quot;)
    x = x - x.groupby(df.week).transform(&amp;quot;mean&amp;quot;)
    return x

y, d = twoway_demean(&amp;quot;sales&amp;quot;), twoway_demean(&amp;quot;treated&amp;quot;)
twfe = (d @ y) / (d @ d)

print(f&amp;quot;True ATT : {df.loc[df.treated == 1, &amp;#39;tau&amp;#39;].mean():.3f}&amp;quot;)
## True ATT : 1.634
print(f&amp;quot;TWFE     : {twfe:.3f}&amp;quot;)
## TWFE     : 0.919&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Same understatement, different random draws. R and NumPy do not share a generator, so the
digits will not match, only the conclusion. For the Callaway-Sant’Anna estimator itself,
Python has &lt;a href=&#34;https://github.com/bernardodionisi/differences&#34;&gt;&lt;code&gt;differences&lt;/code&gt;&lt;/a&gt; and
&lt;a href=&#34;https://pypi.org/project/diff-diff/&#34;&gt;&lt;code&gt;diff-diff&lt;/code&gt;&lt;/a&gt;. The latter also covers synthetic DiD and
Honest DiD sensitivity analysis.&lt;/p&gt;
&lt;/div&gt;
&lt;div id=&#34;what-to-do-on-monday&#34; class=&#34;section level2&#34;&gt;
&lt;h2&gt;What to do on Monday&lt;/h2&gt;
&lt;p&gt;If you are measuring a staggered rollout:&lt;/p&gt;
&lt;ol style=&#34;list-style-type: decimal&#34;&gt;
&lt;li&gt;&lt;strong&gt;Check whether your timing is actually staggered.&lt;/strong&gt; If every market launched the same
week, plain DiD is fine and none of this applies.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Assume your effect is dynamic.&lt;/strong&gt; Awareness builds, and so does the bias. Treating the
effect as one constant number is the assumption that breaks TWFE, not the staggering
itself.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Never let an already-treated market serve as a control.&lt;/strong&gt; This is the whole bug. Use
not-yet-treated or never-treated markets.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Report the event-study curve, not one coefficient.&lt;/strong&gt; The shape is the decision-relevant
output. It tells you the payback period.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Treat flat pre-trends as a check you passed, not a proof.&lt;/strong&gt; Parallel trends is an
assumption about an unobservable counterfactual. If the decision is expensive, run a
sensitivity analysis rather than pointing at the pre-period.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The uncomfortable version of this: if you have been sizing incrementality off staggered
rollouts with a TWFE regression, your campaigns may have looked systematically weaker than
they are, and the budget decisions that followed inherited that error.&lt;/p&gt;
&lt;hr /&gt;
&lt;p&gt;&lt;em&gt;Estimator details, the Goodman-Bacon decomposition, and the assumptions behind each of these
designs are covered in &lt;a href=&#34;https://link.springer.com/book/9783032018380&#34;&gt;Experimental Design&lt;/a&gt;,
Volume 4 of A Guide on Data Analysis.&lt;/em&gt;&lt;/p&gt;
&lt;/div&gt;
</description>
    </item>
    
  </channel>
</rss>
